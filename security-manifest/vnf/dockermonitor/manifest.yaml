manifest:vnsf:
    schema_version: '1.2'
    type: <package data model format (OSM-R2, OSM-R4, ...)>
    package: dockermonitor_vnf.tar.gz
    hash: <sha256-based hash of the .tar.gz package defined above>
    hashing_algorithm: SHA_256
    descriptor: dockermonitor_vnf/dockermonitor_vnfd.yaml
    properties:
        vendor: POLITO
        capabilities: ["monitor"]
    security_info:
        attestation_filename: dockermonitor_vnf_attestation.json
        hash: <sha256-based hash of the .json attestation file defined above>
